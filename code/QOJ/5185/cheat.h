#include <utility>
#include <vector>

std::vector<std::pair<std::pair<int, int>, std::pair<int, int>>> BuildPattern(
    int N);
int GetCardNumber(
    std::vector<std::pair<std::pair<int, int>, std::pair<int, int>>> P);